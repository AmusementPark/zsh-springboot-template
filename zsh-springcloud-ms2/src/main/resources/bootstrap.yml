server:
  port: 9099
spring:
  application:
    name: ms2
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yaml
      discovery:
        server-addr: localhost:8848