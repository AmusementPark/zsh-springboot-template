server:
  port: 9098
spring:
  security:
    user:
      name: root
      password: mima
  application:
    name: ms1
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yaml
      discovery:
        server-addr: localhost:8848
management:
  endpoint:
    shutdown:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
  metrics:
    tags:
      application: actuator-demo-ms